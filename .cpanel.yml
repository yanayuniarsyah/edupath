---
deployment:
  tasks:
    - export DEPLOYPATH=/home/kumonwah/public_html
    - /bin/cp -Rfv dist/* $DEPLOYPATH/
    - /bin/cp -fv dist/.htaccess $DEPLOYPATH/
    - /bin/mkdir -p $DEPLOYPATH/api
    - /bin/cp -Rfv api/* $DEPLOYPATH/api/
    - /bin/cp -fv api/.htaccess $DEPLOYPATH/api/
