package com.edupath.android.core.config

object ApiConfig {
    // 10.0.2.2 is the special alias to your host loopback interface in Android Emulator
    const val BASE_URL = "http://10.0.2.2:8000/api/"
}
